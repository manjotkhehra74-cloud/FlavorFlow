import 'package:flutter/material.dart';

import '../../ui/app_shell.dart';

/// Team — built in Phase 4 (manager today view, members, member day).
class TeamPage extends StatelessWidget {
  const TeamPage({super.key});
  @override
  Widget build(BuildContext context) => const PhasePlaceholder(title: 'Team', icon: Icons.groups_rounded, phase: 4);
}
